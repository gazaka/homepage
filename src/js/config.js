// src/js/config.js

// This is the single source of truth for your proxy server's port.
// If you change the port in docker-compose.yml, you only need to change it here.
export const PROXY_PORT = 2626;
